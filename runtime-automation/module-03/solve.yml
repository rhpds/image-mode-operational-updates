---
# SOLVE — Module 3 (Phase-1 stub)
# Standalone playbook mode. Runs when the learner clicks "Solve".
# Replace with the real participant commands in Phase 2.

- name: "Module 3 Solve"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "Stub solve (no-op)"
      ansible.builtin.debug:
        msg: "module-03 solve stub"
