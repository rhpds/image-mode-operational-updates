---
# VALIDATE — Module 3 (Phase-1 stub)
# Standalone playbook mode. Runs when the learner clicks "Validate".
# Replace with real validation_check tasks in Phase 2.

- name: "Module 3 Validation"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "Stub validation (always passes)"
      validation_check:
        check: true
        pass_msg: "module-03 stub validation passed"
        error_msg: "module-03 stub validation failed"
