---
# SETUP — Module 2 (Phase-1 stub)
# Standalone playbook mode. Runs before the module starts.
# Replace with real logic in Phase 2, after the environment is verified.

- name: "Module 2 Setup"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "Stub setup (no-op)"
      ansible.builtin.debug:
        msg: "module-02 setup stub"
