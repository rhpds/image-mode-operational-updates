---
# VALIDATE — Module 1 (Phase-1 stub)
# Module 1 is introductory and has no validation button per the design spec.

- name: "Module 1 Validation"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "Stub validation (always passes)"
      validation_check:
        check: true
        pass_msg: "module-01 stub validation passed"
        error_msg: "module-01 stub validation failed"
