---
# SOLVE — Module 1 (Phase-1 stub)
# Module 1 is introductory (inspect bootc status); no participant changes.

- name: "Module 1 Solve"
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "Stub solve (no-op)"
      ansible.builtin.debug:
        msg: "module-01 solve stub"
